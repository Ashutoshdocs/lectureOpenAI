# Only needed for the OPTIONAL "Explain (AI)" button, and only when running
# locally with Docker. In-cluster it reads the Kubernetes API, not OpenAI.
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini
