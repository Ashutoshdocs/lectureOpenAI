apiVersion: v1
kind: Service
metadata:
  name: cartographer
  namespace: cartographer
  labels:
    app: cartographer
spec:
  type: NodePort
  selector:
    app: cartographer
  ports:
    - name: http
      port: 80
      targetPort: 8000
      nodePort: 30090      # open at http://<node-ip>:30090
