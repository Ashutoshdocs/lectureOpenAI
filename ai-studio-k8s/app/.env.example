# Local development only. In Kubernetes the key comes from a Secret, not this file.
# Copy to .env for running with plain Docker:  cp .env.example .env
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini
